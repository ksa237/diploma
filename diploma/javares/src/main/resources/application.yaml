spring:
  application:
    name: diploma
  jpa:
    hibernate:
      ddl-auto: none
  sql:
    init:
      mode: never
  datasource:
    url: jdbc:postgresql://postgres:5432/postgres
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

server:
    port: 8080



